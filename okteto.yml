build:
  base:
    context: .
  api:
    context: api
  frontend:
    context: frontend
    dockerfile: Dockerfile
    target: dev
    depends_on: base
    build_args:
      SOURCE_IMAGE: ${OKTETO_BUILD_BASE_IMAGE}
